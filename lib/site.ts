// Single source of truth for the canonical production origin. Every
// canonical URL, sitemap entry, robots host and JSON-LD @id derives from
// this constant — the vercel.app deployment redirects here (next.config.mjs).
export const SITE_URL = "https://webdesignbyivan.de";
